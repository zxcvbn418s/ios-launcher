name: ios_launcher
description: "A beautiful Liquid Glass iOS Launcher built with Flutter in 2026."
publish_to: 'none'
version: 1.0.1+8

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
